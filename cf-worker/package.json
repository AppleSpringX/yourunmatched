{
  "name": "unmatched-cf-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxy that fronts the wispbyte-hosted Node app with HTTPS for Telegram WebApp.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
